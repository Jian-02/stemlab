# extract_mp3.py 는 표준 라이브러리 + ffmpeg(시스템 프로그램)만 사용하므로
# 별도 pip 패키지가 필요 없습니다.
#
# separate_stems.py 는 아래 패키지가 필요합니다.
# (torch, torchaudio 등은 demucs 설치 시 함께 자동으로 설치됩니다)
numpy
demucs
flask

# split_backing_vocal.py (백코러스/하모니 분리)에 필요합니다.
# NVIDIA GPU가 있고 더 빠르게 돌리고 싶다면 "audio-separator[cpu]" 대신
# "audio-separator[gpu]" 로 설치해도 됩니다 (CUDA 필요).
audio-separator[cpu]
# audio-separator가 내부적으로 직접 import하지만 자체 의존성 목록에는
# 빠져있어서, 없으면 ModuleNotFoundError가 난다. 명시적으로 같이 설치.
audioread

# transcribe_vocal.py (보컬 멜로디 자동 채보 -> MIDI/MusicXML 악보)에 필요한
# 패키지는 일부러 여기 넣지 않았습니다. basic-pitch를 그냥 설치하면 기본
# 의존성으로 tensorflow<2.15.1 을 끌고 오는데, 이게 numpy>=2 (위
# audio-separator가 요구)와 호환이 안 될 뿐 아니라 Windows/Python 3.12용
# 배포판 자체가 없어서 설치가 깨집니다 (실제로 겪었던 문제).
#
# 다행히 basic-pitch 안에는 tensorflow 없이도 돌아가는 ONNX 버전 모델이
# 이미 들어있어서 (transcribe_vocal.py가 그걸 사용하도록 되어 있음), 같은
# 이 가상환경에 그대로 설치할 수 있습니다 — 딱 이 순서로 두 번 설치하세요:
#
#     pip install -r requirements-transcribe.txt
#     pip install --no-deps basic-pitch
#
# ("--no-deps"가 핵심입니다 — 이게 없으면 tensorflow까지 같이 끌려와서
# 위에서 설명한 충돌이 그대로 재현됩니다.)